# このファイルをコピーして .env を作る（.env はコミットしない）。
# 本番運用ではシークレット管理(Infisical等)から `infisical run -- <cmd>` で注入する。

# Context7 (最新docs MCP)
CONTEXT7_API_KEY=

# GitHub (gh / GitHub MCP 用。エージェント専用のMachine識別子を使う)
GITHUB_TOKEN=

# Sentry (本番エラー監視)
SENTRY_DSN=

# 各プロジェクト固有の値を以下に追記
